# 2026-02-15 - Mission Control Documentation Sprint & System Recovery

## System Maintenance & Recovery
- **NYT Morning Briefing:** Successful daily run at 05:45 AM. The browser service (`openclaw` profile) was unavailable, so I utilized fallback `web_search` and `web_fetch` to gather the news.
- **Gmail Recovery:** The `gog` Gmail token was revoked. Johan manually refreshed it using `gog auth login` at 08:21 AM. Verification test email was successfully sent.
- **Signal Group Delivery:** Fixed previous issues with Zoidbergs group delivery.

## Mission Control Development (PRs #4–#9)
Completed a major documentation and design review sprint for the Mission Control project.

### PR #4: Robust SSE Replay
- Implemented the "Sequence Cursor" pattern for reliable event delivery.
- Added `sequence_id` (BIGSERIAL) to the `events` table.
- Documented hybrid replay strategy: 100-event Hot Buffer (memory) + Database Fallback (server restarts/long gaps).
- Replaced time-based frontend refresh with explicit `events.reset` signal handling.

### PR #5: Channel Membership Rules
- Formalized access logic: Org-wide channels are open to all; Project channels are restricted to members.
- Added `project_user_assignments` table to manage explicit project membership.
- Membership grants access to the **entire** message history (no time-based filtering).

### PR #6: Redis Key Schemas
- Defined patterns for background jobs (`mc:arq:job:*`), security (`mc:jwt:revoked:*`), and pub/sub (`mc:events:pubsub`).
- Established sizing (256MB–512MB) and eviction policy (`allkeys-lru`).

### PR #7: Search Ranking & Schema Expansion
- Added `description` (markdown) field to the `tasks` table.
- Configured weighted full-text search: Names/Titles (Weight A: 1.0), Descriptions/Content (Weight B: 0.4).
- Documented application-layer boosting (Projects > Tasks > Messages).

### PR #8: Deferred Features (v2+)
- Created a formal list of deferred features to protect MVP scope.
- Includes: dedicated search engine (Typesense), automated cold storage, ABAC permissions, and advanced visualizations (GANTT).

### PR #9: AG Grid Standardization
- Standardized on **AG Grid (Community Edition)** for high-density views: Event Log, User Management, and the Task Table (alternate view).

## Future Reminders
- Monitor the **Tiger Mountain Forecast Watchdog** job (scheduled for 05:05 PM daily).
- Keep an eye on the **openclaw** browser profile stability for automated tasks.
