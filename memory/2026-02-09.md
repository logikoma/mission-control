# 2026-02-09

## Mumble Bridge Audio Optimization
- **Issue:** Audio was "sluggish" (high drift) and choppy.
- **Investigation:** Sub-agent found that `pymumble` has a timing drift bug in its send loop. Also, forcing "voip" Opus profile caused connection instability.
- **Resolution:**
  - Reverted Opus profile to **"audio"** (stable default).
  - Kept frame size at **20ms** (standard).
  - Relying on default encoder bitrate (~50kbps) which is robust.
- **Status:** Bridge restarted (`kind-bloom`) with stable settings. User to verify if "sluggishness" is reduced.

## Daily Briefing
- **Status:** Successfully delivered to Signal DM, Email, and Zoidbergs Group.
- **Group Delivery:** Had to use `sessions_send` with the group session key because `message` tool failed on Base64 Group ID.
- **Refinement:** Added "Do not summarize" instruction to group delivery to preserve links.
- **Dependencies:** Still need Brave Search API Key for headless reliability.

## Infrastructure
- **Mumble Server:** Now running as a system service (`launchctl`).
- **Web Search:** Brave Search enabled and verified working.
